---
title: "Domain-Driven Design (DDD)"
emoji: "📝"
type: "tech" # tech: 技術記事 / idea: アイデア
topics: [""]
published: false
---

# Lời mở đầu

DDD không phải “khuôn mẫu framework”, mà là cách tổ chức tư duy xoay quanh miền nghiệp vụ. Bài này tóm tắt khái niệm cốt lõi, những quy tắc nên giữ, cách tư duy để code không bị rối khi dự án lớn dần, và 1 use case áp dụng vào codebase Rust theo cấu trúc:

```
domain/          # model (Entity/VO), repository (ports), domain services
application/     # usecases (orchestration, transaction boundary)
infrastructure/  # adapters (DB/Keycloak/Mail/Storage/Webhook)
interface/       # web handlers, middleware, routes, batch
registry/        # wiring/DI: bind ports ↔ adapters
shared/          # cross-cutting: error, time, ids...
```

# DDD là gì? Tại sao nên dùng thay vì cấu trúc khác?

**Định nghĩa ngắn:** Thiết kế phần mềm xoay quanh Ubiquitous Language (ngôn ngữ chung) và domain model, tách biệt domain khỏi framework/hạ tầng.

**Lợi ích thực tế:**

- Domain “sạch”, test được không cần DB/HTTP.

- Thay thế hạ tầng dễ (Postgres ↔ MySQL, SMTP ↔ SES, Keycloak ↔ Cognito…).

- Giảm rối ở handler: request/response mỏng, nghiệp vụ nằm đúng chỗ.

- Sống tốt với dự án dài hơi, nhiều feature, nhiều team.

**Khi nào không dùng?**

Ứng dụng rất nhỏ/vòng đời ngắn, quy tắc nghiệp vụ gần như không có → overhead chia layer có thể là phí.
